{
  "name": "inventario-ganaderia-api",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "dev": "$repo=(Get-Location).ProviderPath; if ($repo.StartsWith('\\\\?\\')) { $repo=$repo.Substring(4) }; Set-Location -LiteralPath $repo; node ../../scripts/run-local-bin.cjs tsx watch src/index.ts",
    "build": "$repo=(Get-Location).ProviderPath; if ($repo.StartsWith('\\\\?\\')) { $repo=$repo.Substring(4) }; Set-Location -LiteralPath $repo; node ../../scripts/run-local-bin.cjs tsc -p tsconfig.build.json",
    "start": "$repo=(Get-Location).ProviderPath; if ($repo.StartsWith('\\\\?\\')) { $repo=$repo.Substring(4) }; Set-Location -LiteralPath $repo; node ../../scripts/run-local-bin.cjs node dist/index.js",
    "lint": "$repo=(Get-Location).ProviderPath; if ($repo.StartsWith('\\\\?\\')) { $repo=$repo.Substring(4) }; Set-Location -LiteralPath $repo; node ../../scripts/run-local-bin.cjs tsc --noEmit -p tsconfig.json",
    "prisma:generate": "$repo=(Get-Location).ProviderPath; if ($repo.StartsWith('\\\\?\\')) { $repo=$repo.Substring(4) }; Set-Location -LiteralPath $repo; node ../../scripts/run-local-bin.cjs prisma generate",
    "prisma:migrate": "$repo=(Get-Location).ProviderPath; if ($repo.StartsWith('\\\\?\\')) { $repo=$repo.Substring(4) }; Set-Location -LiteralPath $repo; node ../../scripts/run-local-bin.cjs prisma migrate dev",
    "prisma:deploy": "$repo=(Get-Location).ProviderPath; if ($repo.StartsWith('\\\\?\\')) { $repo=$repo.Substring(4) }; Set-Location -LiteralPath $repo; node ../../scripts/run-local-bin.cjs prisma migrate deploy",
    "seed": "$repo=(Get-Location).ProviderPath; if ($repo.StartsWith('\\\\?\\')) { $repo=$repo.Substring(4) }; Set-Location -LiteralPath $repo; node ../../scripts/run-local-bin.cjs tsx prisma/seed.ts",
    "pretest": "$repo=(Get-Location).ProviderPath; if ($repo.StartsWith('\\\\?\\')) { $repo=$repo.Substring(4) }; Set-Location -LiteralPath $repo; node ../../scripts/run-local-bin.cjs prisma generate",
    "test": "$repo=(Get-Location).ProviderPath; if ($repo.StartsWith('\\\\?\\')) { $repo=$repo.Substring(4) }; Set-Location -LiteralPath $repo; node ../../scripts/run-local-bin.cjs vitest run"
  },
  "dependencies": {
    "@prisma/client": "^5.19.1",
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "csv-parse": "^5.5.6",
    "dayjs": "^1.11.11",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.3.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "pino": "^9.3.2",
    "pino-http": "^10.2.0",
    "swagger-ui-express": "^5.0.0",
    "yaml": "^2.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/cookie-parser": "^1.4.7",
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.6",
    "@types/multer": "^1.4.12",
    "@types/node": "^20.14.9",
    "@types/supertest": "^2.0.16",
    "prisma": "^5.19.1",
    "supertest": "^7.0.0",
    "tsx": "^4.16.5",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
